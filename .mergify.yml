commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

scopes:
  source:
    manual:
  default_capacity: 1

queue_rules:
  - name: vm3-future-barrier
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-future-barrier-hold
    merge_method: merge

merge_queue:
  mode: parallel
  max_parallel_checks: 2
